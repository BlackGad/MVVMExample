<ContentControl x:Class="MVVMExample.Views.Area1View"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:viewModels="clr-namespace:MVVMExample.ViewModels"
                d:DataContext="{d:DesignInstance viewModels:Area1ViewModel}"
                Focusable="False"
                mc:Ignorable="d">
    <Grid Background="Azure">
        <TextBlock HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Text="{Binding CalculatedValue, StringFormat={}Value from view model: {0}}" />
    </Grid>
</ContentControl>
